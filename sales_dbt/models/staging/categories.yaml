models:
  - name: categories
    columns:
      - name: category_id
        tests:
          - unique
          - not_null

      - name: category_name
        description: product category name
        tests:
          - not_null